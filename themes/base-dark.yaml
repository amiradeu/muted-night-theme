name: Muted Night
type: dark
tokenColors:
    # ------------------------------------------------
    #    Comment
    # ------------------------------------------------
    - name: Comment
      scope: comment
      settings:
          fontStyle: italic
          foreground: _comment-gray
    # ------------------------------------------------
    #    String
    # ------------------------------------------------
    - name: String
      scope: string
      settings:
          foreground: _gold
    # ------------------------------------------------
    #    Constant
    # ------------------------------------------------
    - name: Constant
      scope: constant, punctuation.definition.template-expression
      settings:
          foreground: _orange
    # ------------------------------------------------
    #    Variable
    # ------------------------------------------------
    - name: Variable
      scope: variable
      settings:
          foreground: _white
    # ------------------------------------------------
    #    Variable Parameter
    # ------------------------------------------------
    - name: Variable
      scope: variable.parameter
      settings:
          fontStyle: italic
          foreground: _code-gray
    # ------------------------------------------------
    #    Keyword
    # ------------------------------------------------
    - name: Keyword
      scope: keyword.control
      settings:
          foreground: _pink
    # ------------------------------------------------
    #    Function
    # ------------------------------------------------
    - name: Function
      scope: meta.function
      settings:
          foreground: _yellow
    # ------------------------------------------------
    #    Attribute
    # ------------------------------------------------
    - name: Attribute
      scope: entity.other.attribute-name
      settings:
          foreground: _blue
    # ------------------------------------------------
    #    Punctuation
    # ------------------------------------------------
    - name: Punctuation
      scope: punctuation.definition.expression
      settings:
          foreground: _green
